# QA V16.1.2 — Archives mondiales figées

## Périmètre

La recette porte sur la restauration de trois archives identiques sur chaque page pays, placées après le récit :

- classement final des 12 groupes ;
- classement final des 8 meilleurs troisièmes ;
- tableau final de la Coupe du monde 2026.

## Tests automatisés

### Lot A — non-régression

- routeur explicite home / worldcup / news / gaindes / final / team ;
- nouvelle home conservée ;
- Mémoire du Mondial conservée ;
- CMS et libellé « L’analyse de la rédaction » conservés ;
- ordre des Brèves conservé ;
- ancien mode global non réintroduit.

Résultat : **PASS**.

### Lot B — pages pays et archives

- 16 pays ;
- 5 langues ;
- 80 combinaisons rendues ;
- trois tableaux du parcours propre au pays conservés ;
- récit placé avant les archives mondiales ;
- trois archives mondiales présentes ;
- données identiques sur toutes les pages en français ;
- 12 groupes ;
- 8 meilleurs troisièmes ;
- 32 matchs du tableau final ;
- finale N104 présente ;
- aucune expression de simulation dans le renderer actif.

Résultat : **PASS**.

## Test navigateur

### Mémoire du Mondial

- rendu dans le root applicatif dédié ;
- 16 équipes accessibles ;
- absence de reprise du gabarit Belgique–Sénégal.

Résultat : **PASS**.

### Page pays mobile

Viewport testé : 390 × 844.

- ordre visuel correct ;
- 12 cartes de groupes ;
- 8 lignes de meilleurs troisièmes ;
- 32 cartes de matchs ;
- tableau final horizontalement défilable ;
- aucune erreur JavaScript ;
- aucun texte « simulation » visible.

Résultat : **PASS**.

## Contrôles techniques

- `npm ci --ignore-scripts` : réussi ;
- `npm test` : réussi ;
- syntaxe du renderer V16.1.2 : valide ;
- syntaxe des scripts inline d'`index.html` : valide ;
- JSON des données : valide ;
- `index.html` à la racine : confirmé ;
- `node_modules` exclu du ZIP final : confirmé avant livraison ;
- registre npm public dans le lockfile : confirmé.

## Note dépendances

`npm audit` signale 6 avis de sévérité modérée dans l'arbre transitif existant de `@netlify/blobs`. La V16.1.2 n'ajoute aucune nouvelle dépendance et ne modifie pas l'authentification ou le stockage CMS.

## Test bloquant final

```text
Mémoire du Mondial
→ Espagne
→ récit
→ 12 groupes
→ 8 meilleurs troisièmes
→ tableau final
→ retour Mémoire
→ Sénégal
→ mêmes trois archives
→ aucun contrôle de simulation
```

Résultat : **PASS**.
