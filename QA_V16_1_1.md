# QA V16.1.1 — Suivi des Lions

## Lot A — Home, CMS, navigation et Mémoire du Mondial

Statut : **PASS**

Contrôles automatisés :

- classification explicite des routes home, worldcup, global, news, gaindes, final et team ;
- ancien sélecteur non réactivé ;
- présence du root dédié `#qg-app-root` ;
- quarantaine visuelle de tout le HTML legacy hors équipes ;
- rendu réel de `?mode=worldcup` dans le nouveau root ;
- 16 cartes équipes ;
- absence de « Belgique vs Sénégal », « Prochaine rencontre des Gaïndés » et « Voir la page globale » dans la Mémoire neutre ;
- wording « Votre App’ “Suivi des Lions” reste au cœur du jeu » ;
- absence de QualifGaïndé dans la home active ;
- ordre des brèves du plus récent au plus ancien ;
- titre Brèves des Gaïndés réduit ;
- étiquette de la carte Brèves en blanc ;
- libellé CMS « L’analyse de la rédaction ».

Contrôles navigateur headless sur le vrai `index.html` :

- home V16.1.1 visible et ancien sélecteur masqué ;
- `?mode=worldcup&v=1611` visible avec 16 nations ;
- page Espagne visible avec trois tableaux ;
- aucune fuite Belgique–Sénégal sur les routes testées.

## Lot B — Pages équipe et tableaux

Statut : **PASS**

- 16 équipes ;
- 5 langues : FR, EN, ES, PT, AR ;
- 80 combinaisons équipe/langue ;
- exactement trois tableaux par page : groupes, élimination directe, parcours complet ;
- navigation vers la Mémoire du Mondial et l’accueil ;
- absence de fallback Sénégal et d’assets Sénégal sur les autres équipes ;
- aucun appel public à l’ancien `updateTeamPage()`.

## Contrôles techniques

- `npm ci --ignore-scripts` : PASS ;
- `npm test` : PASS ;
- syntaxe de `computed-team-state.js`, `team-page-v1611.js`, `gaindes-gate.js` et `news-cms.mjs` : PASS ;
- syntaxe des 12 scripts inline de `index.html` : PASS ;
- 65 fichiers JSON applicatifs valides après exclusion de `node_modules` ;
- registre du lockfile : `registry.npmjs.org` uniquement ;
- `index.html` à la racine ;
- `node_modules` exclu du ZIP final.

## Point à valider après déploiement Netlify

Effectuer une recette réelle avec un rechargement forcé :

1. ouvrir `/?v=1611` ;
2. cliquer sur « Entrer dans la mémoire » ;
3. vérifier l’URL `?mode=worldcup&v=1611` ;
4. ouvrir Espagne puis revenir à la Mémoire ;
5. publier ou modifier une brève dans la console ;
6. vérifier que les articles déjà présents dans Netlify Blobs sont conservés.

## Dépendances

`npm audit` signale 6 avis de sévérité modérée dans l’arbre de dépendances de `@netlify/blobs`. Ils n’ont pas provoqué d’échec d’installation, de syntaxe ou de test dans cette version. Aucun correctif forcé potentiellement cassant n’a été appliqué.
