# QA — QualifGaïndé V15.4.1

## Contrôles techniques

- Syntaxe JavaScript de `assets/js/computed-team-state.js` validée avec Node.
- 64 fichiers JSON validés.
- Présence vérifiée des fichiers racine essentiels :
  - `index.html` ;
  - `assets/js/computed-team-state.js` ;
  - `netlify/functions/scores.js` ;
  - `service-worker.js` ;
  - `live.json`.
- Comparaison SHA-256 avec la V15.4.0 :
  - `netlify/functions/scores.js` inchangé ;
  - `live.json` inchangé ;
  - `data/matches.json` inchangé ;
  - `data/team-results.json` inchangé.
- Aucun fichier supprimé par rapport au build complet V15.4.0.

## Contrôles visuels

- Structure statique rendue dans Chromium pour contrôler la galerie.
- 13 équipes affichées dans une grille de 4 colonnes sur grand écran.
- Le dialogue n’utilise plus de `max-height` ni de `overflow:auto` interne.
- La barre de défilement de l’overlay est masquée.
- Les cartes restent cliquables et conservent leurs liens équipe.
- Les thèmes colorimétriques sont appliqués par classe, sans modification du renderer live.

## Points à vérifier après déploiement

- Home : contraste des cartes sur desktop et mobile.
- Ouverture/fermeture de « Les équipes qui nous ont fait vibrer ».
- Navigation vers chaque page équipe depuis la galerie.
- Home et Suivi des Gaïndés après un rechargement forcé.
